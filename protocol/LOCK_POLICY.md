# Confirmatory V3 lock policy
The confirmatory run is forbidden until the development report, dataset IDs, case-study decision rule, code, configs, tests, and environment lock are complete. `freeze_protocol_v3.py` creates the immutable manifest. Authorization is a separate manual act and must not be inferred from creation of the lock file.
